class Solution:
    def isPowerOfTwo(self, n: int) -> bool:
        for i in range(0,31):
            if pow(2,i) != n:
                continue
            else: 
                return True
        return False
