class Solution:
    def isPowerOfThree(self, n: int) -> bool:
        for i in range(0,31):
            if pow(3,i) != n:
                continue
            else:
                return True
        else:
            return False
