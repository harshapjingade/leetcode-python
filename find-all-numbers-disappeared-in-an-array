class Solution:
    def findDisappearedNumbers(self, nums: List[int]) -> List[int]:
        newlist=[]
        l = set(nums)
        for i in range(1,len(nums)+1):
            if i not in l:
                newlist.append(i)
        return newlist
