class Solution:
    def addToArrayForm(self, num: List[int], k: int) -> List[int]:
        astring=""
        newlist=[]
        for i in num:
            astring+=str(i)
        total=int(astring)+k
        for i in str(total):
            newlist.append(int(i))
        return newlist
            
