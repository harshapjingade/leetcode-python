class Solution:
    def isPowerOfFour(self, n: int) -> bool:
        for i in range(0,31):
            if pow(4,i) != n:
                continue
            else:
                return True
        else:
            return False
